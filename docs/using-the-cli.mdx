# Using the CLI Tool

You can use the Rensa CLI tool to quickly spin up a new project quickly with minimal manual setup required.

## Starting the CLI

Open your terminal and navigate to the directory where the new project is to be created. Then, run the following command in the terminal :
```npm
npx rensa
```
Or,
```yarn
yarn dlx rensa
```
